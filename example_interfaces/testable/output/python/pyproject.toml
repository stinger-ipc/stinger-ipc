[project]
name = "testable-ipc"
version = "0.0.1"
description = "Interface for testing"
requires-python = ">=3.9"
dependencies = [
    "pydantic>=2.11.7",
    "paho-mqtt>=2.0.1",
    "isodate>=0.7.2",
    "textual>=6.6.0",
]

[project.optional-dependencies]
tui = [
    "textual",
]
