stingeripc: 0.0.2

interface:
  name: SignalOnly
  version: 0.0.1

signals:

  theSignal:
    payload:
      schema:
        $ref: "schemas.yaml#/signals/theSignal"
  
  anotherSignal:
    payload:
      args:
        one: float
        two: bool
        three: string

