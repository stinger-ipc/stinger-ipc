{%if not config.python.python37%}
# Configuration file for the Sphinx documentation builder.
#
# This documents the generated `{{stinger.python.package_name}}` package, not
# stinger-ipc itself. It is regenerated on every `stinger generate` run.

import os
import sys
from datetime import datetime, UTC

sys.path.insert(0, os.path.abspath("../src"))

project = "{{stinger.title or stinger.name}}"
copyright = f"{datetime.now(UTC).year}"
author = "{{stinger.python.package_name}}"
release = "{{stinger.version}}"
version = release

extensions = [
    "sphinx.ext.autodoc",
    "sphinx.ext.autosummary",
    "sphinx.ext.napoleon",
    "sphinx.ext.viewcode",
    "sphinx.ext.intersphinx",
    "sphinx_autodoc_typehints",
]

templates_path = []
exclude_patterns = ["_build", "Thumbs.db", ".DS_Store"]

# sphinx_autodoc_typehints warns about pydantic-internal forward references and
# guarded imports that aren't part of this package's own code.
suppress_warnings = [
    "sphinx_autodoc_typehints.forward_reference",
    "sphinx_autodoc_typehints.guarded_import",
]

html_theme = "sphinx_rtd_theme"
html_static_path = []

autodoc_default_options = {
    "members": True,
    "member-order": "bysource",
    "show-inheritance": True,
}
add_module_names = False
autosummary_generate = True

napoleon_google_docstring = True
napoleon_numpy_docstring = False

intersphinx_mapping = {
    "python": ("https://docs.python.org/3", None),
    "pydantic": ("https://docs.pydantic.dev/latest/", None),
}
{%endif%}
