{%- macro argrow(arg) -%}
|{{arg.name|center(15)}}|
{%-if arg.arg_type.name.lower() == "enum" or arg.arg_type.name.lower() == "struct"%}{{arg.markdown.markdown_type|center(10)}}
{%-else%}{{arg.json_type|center(10)}}
{%-endif%}{%if arg.optional%} (optional){%endif%}|
{{- arg.description or ''}}|
{%- endmacro -%}
{%- macro argtable(arglist) -%}
| Name          | Type     |Description|
|---------------|----------|-----------|
{%-for arg in arglist%}
{{argrow(arg)}}
{%- endfor %}
{%- endmacro -%}
# _{{stinger.title}}_ API Overview 

<!--
This is automatically generated documentation.
{%if stinger.license%}LICENSE: {{stinger.license}}{%endif%}
-->

{%-if stinger.summary %} 
{{stinger.summary|italics}}
{%endif-%}
{%if stinger.documentation%}
{{stinger.documentation}}
{%endif%}

[[_TOC_]]

## Connections

A connection object is a wrapper around an MQTT client and provides specific functionality to support both clients and servers.
Generally, you only need one connection object per daemon/program, as it can support multiple clients and servers.  
For most languages, Stinger-IPC does not require a specific connection object implementation, as long as it implements the required interface.

The application code is responsible for creating and managing the connection object, including connecting to the MQTT broker.

## Discovery

Because there may be multiple instances of the same Stinger Interface, a discovery mechanism is provided to find and connect to them.  A discovery class is provided which connects to
the MQTT broker and listens for Stinger Interface announcements.  The discovery class can then provide client instances.  Additionally, the discovery class
find all the current property values for discovered interfaces in order to initialize the client instance.

## Server

A server is a _provider_ of functionality.  It {%if stinger.signals|length > 0%}sends signals {%endif%}{%if stinger.methods|length > 0 %}and handles method calls {%endif%}{%if stinger.commands|length > 0 %}and receives commands {%endif%}{%if stinger.properties|length > 0 %}and owns property values{%endif%}.

When constructing a server instance, a connection object{%if stinger.properties | length > 0 %} and initial property values must be provided{%endif%}.

## Client

A client is a _utilizer_ of functionality.  It receives signals, makes method calls, sends commands, reads property values, or requests updates to property values.


{%if stinger.signals | length > 0 %}
## Signals

Signals are messages from a server to clients.

```plantuml
@startuml
Client <<- Server : Signal(Parameters)
@enduml
```
{%for sig_name, signal in stinger.signals.items()%}
### Signal `{{sig_name}}`

{{signal.documentation or "_No documentation for this signal_"}}

#### Signal Parameters for `{{sig_name}}`

{{argtable(signal.arg_list)}}

{%endfor%}{#- end for each signal -#}
{%endif%}{#- end if there are signals -#}

{#- ------------------------------------------------------- -#}
{%if stinger.methods | length > 0 %}
## Methods

Methods are requests from a client to a server and the server provides a response back to the client:

```plantuml
@startuml
Client ->> Server : Request(Parameters)
Client <<-- Server: Response(Parameters)
@enduml
```

{%for method_name, method in stinger.methods.items()%}
### Method `{{method_name}}`

{{method.documentation or "_No documentation for this method_"}}

#### Request Parameters
{%if method.arg_list | length == 0%}
There are no arguments for this request.
{%else-%}
{{argtable(method.arg_list)}}
{%endif%}{# number of args #}
#### Return Value

{%if method.return_value is none -%}
There is no return value for this method call.  An empty response object will still be sent from server to client.
{%else-%}
{{argtable(method.return_arg_list)}}
{%endif-%}

{%endfor-%}
{%endif%}
{#- ------------------------------------------------------- -#}
{%if stinger.commands | length > 0 %}
## Commands

Commands are messages from a client to a server.  A command is the mirror image of a signal: it travels client to server instead of server to client.  Put another way, it is a method call with no response.
Commands are fire-and-forget.  There is no response message, so a client never learns whether the server acted on the command, and the server never learns which client sent it.

```plantuml
@startuml
Client ->> Server : Command(Parameters)
@enduml
```
{%for command_name, command in stinger.commands.items()%}
### Command `{{command_name}}`

{{command.documentation or "_No documentation for this command_"}}

* Topic: `{{command.topic()}}`
* Declared version: {%if command.version%}`{{command.version}}`{%else%}_none declared_{%endif%}

#### Command Parameters for `{{command_name}}`
{%if command.arg_list | length == 0%}
There are no arguments for this command.
{%else-%}
{{argtable(command.arg_list)}}
{%endif%}{# number of args #}
{%endfor%}{#- end for each command -#}
{%endif%}{#- end if there are commands -#}
{#- ------------------------------------------------------- #}
{%if stinger.properties | length > 0 %}## Properties

Properties are values held by the server.  They are re-published when the value changes.  Each property holds exactly one value, but that value can be an array or a struct.

```plantuml
@startuml
Server -> Server : Set Property
Client <<- Server: Property Updated
@enduml
```
{%for prop_name, prop in stinger.properties.items()%}
### Property `{{prop_name}}`

{{prop.documentation or "_No documentation is available for this property_"}}{%if prop.read_only %}

This property is **read-only**.  It can only be modified by the server.{%endif%}

{{argtable(prop.arg_list)}}

{%endfor%}{# for each property #}
{%endif%}{#end condition that there are a number of properties#}
{%if stinger.enums | length > 0%}## Enums
{%endif%}

{%for ie_name, ie in stinger.enums.items() %}
{#- ------------------------------------------------------- -#}
### Enum `{{ie.class_name }}`

<a name="Enum-{{ie.class_name}}"></a>{{ie.documentation or "_No description exists for this enumeration._"}}

{%for enum_item in ie.items -%}
* {{enum_item.name}} ({{enum_item.integer}}){%- if enum_item.description %} - {{enum_item.description}}{% endif %}
{%endfor%}
{%endfor%}
{#- ------------------------------------------------------- #}
## Structures

Structures are a group of values and may be used as an argument in signals, methods, or properties.  Defining a structure allows for easy reuse.
{%for istruct_name, istruct in stinger.structs.items() %}
### Struct `{{istruct.class_name }}`

<a name="Struct-{{istruct.class_name}}"></a>{{istruct.description or "_No general description exists for this structure_"}}

{{argtable(istruct.values)}}
{%endfor%}
