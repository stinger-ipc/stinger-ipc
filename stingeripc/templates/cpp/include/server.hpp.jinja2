/*
DO NOT MODIFY THIS FILE.  It is automatically generated and changes will be over-written
on the next generation.

It contains enumerations used by the {{stinger.name}} interface.

{%if stinger.license%}LICENSE: {{stinger.license}}{%endif%}
*/
{% import 'partials/properties.jinja2' as pr %}

#pragma once

#include <cstdint>
#include <functional>
#include <map>
#include <string>
#include <memory>
#include <exception>
#include <mutex>
#include <chrono>
#include <thread>
#include <atomic>
#include <rapidjson/document.h>
{%if stinger.properties | length > 0 %}
#include "property_structs.hpp"
{%endif%}
#include <stinger/utils/iconnection.hpp>
#include <stinger/mqtt/properties.hpp>
#include "{{stinger.cpp.enum_header_file}}"
{%if stinger.methods | length > 0%}
#include "method_payloads.hpp"
{%endif%}
{%if stinger.signals | length > 0%}
#include "signal_payloads.hpp"
{%endif%}
{%- import "partials/codec.jinja2" as codec %}
{{- codec.imports(stinger) }}
{%if stinger.commands | length > 0%}
#include "command_payloads.hpp"
{%endif%}

{%for ns in config.cpp.namespace%}
namespace {{ns}} {
{%endfor-%}
namespace {{stinger.name|snake_case}} {

class {{stinger.cpp.server_class_name}} {

public:
    static constexpr const char NAME[] = "{{stinger.name}}";
    static constexpr const char INTERFACE_VERSION[] = "{{stinger.version}}";

    {{stinger.cpp.server_class_name}}(std::shared_ptr<stinger::utils::IConnection> broker, const std::string& instanceId{%for topic_param in config.topics.params %}, const std::string& {{topic_param | lowerCamelCase}}{%endfor%});

    virtual ~{{stinger.cpp.server_class_name}}();

    {%for sig_name, sig in stinger.signals.items()%}
    std::future<bool> emit{{sig_name | UpperCamelCase}}Signal({%for arg in sig.arg_list%}{{arg.cpp.func_param_type}}{%if not loop.last%}, {%endif%}{%endfor%});

    // Publishes an already-built '{{sig_name}}' payload, for callers that hold the
    // payload object rather than its individual values.
    std::future<bool> emit{{sig_name | UpperCamelCase}}SignalPayload(const {{sig.payload.cpp.class_name}}& payload);
    {%endfor%}

    {%for method_name, method in stinger.methods.items()%}
    void register{{method_name | UpperCamelCase}}Handler(std::function<{{method.cpp.return_value_class}}({%for arg in method.arg_list%}{{arg.cpp.temp_type}}{%if not loop.last%}, {%endif%}{%endfor%})> func);
    {%endfor%}

    {%for command_name, command in stinger.commands.items()%}
    // Register a handler for the `{{command_name}}` command.
    // Any number of handlers may be registered; each one is called when a `{{command_name}}`
    // command is received.  Commands are fire-and-forget, so a handler returns nothing and
    // nothing is sent back to the client that sent the command.
    void receive{{command_name | UpperCamelCase}}Command(const std::function<void({%for arg in command.arg_list%}{{arg.cpp.temp_type}}{%if not loop.last%}, {%endif%}{%endfor%})>& handler);

    // Registers a handler that receives the whole '{{command_name}}' payload rather than
    // its individual arguments.  Any number of handlers of either kind may be registered.
    void receive{{command_name | UpperCamelCase}}CommandPayload(const std::function<void(const {{command.payload.cpp.class_name}}&)>& handler);
    {%endfor%}

    {%for prop_name, prop in stinger.properties.items()%}
    {{ pr.public_declarations(prop_name, prop, false, config) | indent(4) }}
    void republish{{prop.name | UpperCamelCase}}Property() const;
    {%endfor%}
private: 
    std::shared_ptr<stinger::utils::IConnection> _broker;
    std::string _instanceId;
    {%for topic_param in config.topics.params %}
    std::string _{{topic_param | lowerCamelCase}}TopicParam;
    {%endfor%}
    stinger::utils::CallbackHandleType _brokerMessageCallbackHandle = 0;
    void _receiveMessage(const stinger::mqtt::Message& msg);

    {%for method_name, method in stinger.methods.items()%}
    void _call{{method_name | UpperCamelCase}}Handler(const std::string& topic, const std::string& payload, const std::optional<std::vector<std::byte>>& optCorrelationData, const std::optional<std::string>& optResponseTopic) const;
    std::function<{{method.cpp.return_value_class}}({%for arg in method.arg_list%}{{arg.cpp.temp_type}}{%if not loop.last%}, {%endif%}{%endfor%})> _{{method_name | camelCase}}Handler;
    int _{{method_name | camelCase}}MethodSubscriptionId;
    {%endfor%}

    {%if stinger.commands|length > 0 -%}
    // ------------------- COMMANDS -------------------
    {%for command_name, command in stinger.commands.items()%}
    // Internal method for handling a received `{{command_name}}` command.
    std::vector<std::function<void(const {{command.payload.cpp.class_name}}&)>> _{{command_name | lowerCamelCase}}CommandPayloadHandlers;
    void _receive{{command_name | UpperCamelCase}}Command(const stinger::mqtt::Message& msg);

    // List of handlers to be called whenever the `{{command_name}}` command is received.
    // An empty list is legal; received commands are then dropped.
    std::vector<std::function<void({%for arg in command.arg_list%}{{arg.cpp.temp_type}}{%if not loop.last%}, {%endif%}{%endfor%})>> _{{command_name | lowerCamelCase}}CommandHandlers;
    std::mutex _{{command_name | lowerCamelCase}}CommandHandlersMutex;

    // MQTT Subscription ID for `{{command_name}}` command receptions.
    int _{{command_name | lowerCamelCase}}CommandSubscriptionId = -1;
    {%endfor%}
    {%endif%}

    {%if stinger.properties|length > 0 -%}
    // ---------------- PROPERTIES ------------------
    {%for prop_name, prop in stinger.properties.items()%}
    {{pr.private_declarations(prop_name, prop, false, config) | indent(4)}}
    {%endfor%}
    {%endif%}

    // ---------------- SERVICE ADVERTISEMENT ------------------

    // Thread for publishing service advertisement messages
    std::thread _advertisementThread;

    // Flag to signal the advertisement thread to stop
    std::atomic<bool> _advertisementThreadRunning;

    // Method that runs in the advertisement thread
    void _advertisementThreadLoop();
};

} // namespace {{stinger.name|snake_case}}
{%for ns in config.cpp.namespace|reverse%}
} // namespace {{ns}}
{%endfor-%}