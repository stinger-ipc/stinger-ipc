{% import "partials/deserialize.jinja2"  as deser %}
{% import "partials/serialize.jinja2" as ser %}
{% import "partials/validate.jinja2" as val %}
#include "property_structs.hpp"
#include <rapidjson/document.h>
#include <rapidjson/schema.h>

{%for ns in config.cpp.namespace%}
namespace {{ns}} {
{%endfor-%}
namespace {{stinger.name|snake_case}} {

{%for prop_name, prop in stinger.properties.items()%}
{%- if not prop.payload.is_protobuf %}
{{prop.payload.cpp.class_name}} {{prop.payload.cpp.class_name}}::FromRapidJsonObject(const rapidjson::Value& jsonObj)
{
    {{prop.payload.cpp.class_name}} {{prop.name | lowerCamelCase }};
    {{deser.deserialize(prop.name|lowerCamelCase, prop.arg_list, 'jsonObj') | indent(4)}}

    return {{prop.name | lowerCamelCase}};
};

void {{prop.payload.cpp.class_name}}::AddToRapidJsonObject(rapidjson::Value& parent, rapidjson::Document::AllocatorType& allocator) const
{
    {%for arg in prop.arg_list%}
    {{ser.addToValue('parent', arg, 'allocator') | indent(4)}}
    {%- endfor %}
}
{{ val.validate_schema_body((prop.name | UpperCamelCase) ~ "Property", prop.arg_list) }}
{%- endif %}
{% endfor %}

} // namespace {{stinger.name|snake_case}}
{%for ns in config.cpp.namespace|reverse%}
} // namespace {{ns}}
{%endfor-%}