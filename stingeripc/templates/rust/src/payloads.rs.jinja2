{%- from "partials/codec.jinja2" import wire_trait, wire_macros, wire_impls -%}
//! Payloads module for {{stinger.name}} IPC
//! 
//! Contains all the data structures, enums, and return codes used by the {{stinger.name}} IPC system.

/*
DO NOT MODIFY THIS FILE .  It is automatically generated by Stinger-IPC {{generator.version}} and changes will be over-written
on the next generation.

It contains enumerations used by the {{stinger.name}} interface.

{%if stinger.license%}LICENSE: {{stinger.license}}{%endif%}
*/

{%if stinger.enums | length > 0 %}
use num_derive::{FromPrimitive, ToPrimitive};
use num_traits::FromPrimitive;
use std::fmt;
{%endif%}
use serde::{Deserialize, Serialize};
use bytes::Bytes;
{%- if stinger.uses_protobuf() %}
use bytes::BytesMut;
{%- endif %}

// The custom serde (de)serialization helpers live in their own module so this
// file stays focused on the payload data structures.  Each import is only
// emitted when the interface actually contains a field of that type.
{%- if stinger.uses_binary() %}
use crate::serde_formats::base64_binary_format;
{%- endif %}
{%- if stinger.uses_datetime() %}
use crate::serde_formats::datetime_iso_format;
{%- endif %}
{%- if stinger.uses_duration() %}
use crate::serde_formats::duration_iso_format;
{%- endif %}


{{ wire_trait() }}
{{ wire_macros(stinger) }}

#[allow(dead_code)]
#[derive(Debug, Clone)]
pub enum MethodReturnCode { {%-for rc_i, rc_name in stinger.method_return_codes.items()%}
    {{rc_name | UpperCamelCase}}{%if not loop.first%}(String){%else%}(Option<Bytes>){%endif%},
    {%-endfor%}
}

impl MethodReturnCode {
    pub fn from_code(code: u32, message: Option<String>) -> Self {
        match code {
            {%-for rc_i, rc_name in stinger.method_return_codes.items()%}
            {{rc_i}} => MethodReturnCode::{{rc_name | UpperCamelCase}}({%if loop.first%}message.map(Bytes::from){%else%}message.unwrap_or_default(){%endif%}),
            {%-endfor%}
            _ => MethodReturnCode::UnknownError(message.unwrap_or_default()),
        }
    }

    pub fn to_code(&self) -> (u32, Option<String>) {
        match self {
            {%-for rc_i, rc_name in stinger.method_return_codes.items() %}
            MethodReturnCode::{{rc_name | UpperCamelCase}}{%if not loop.first%}(msg){%else%}(_){%endif%} => ({{rc_i}}, {%if not loop.first%}Some(msg.clone()){%else%}None{%endif%}),
            {%-endfor%}
        }
    }
}

// --- ENUMERATIONS ---

{%for ie_name, ie in stinger.enums.items() %}
#[repr(i32)]
#[derive(Debug, FromPrimitive, ToPrimitive, Clone, Serialize, Deserialize, PartialEq)]
#[serde(into = "i32", try_from = "i32")]
pub enum {{ie_name | UpperCamelCase }} {
    {%-for enum_item in ie.items %}
    {%- if enum_item.description %}
    /// {{ enum_item.description }}
    {%- endif %}
    {{enum_item.name | UpperCamelCase}} = {{enum_item.integer}}{%if not loop.last%},{%endif%}
    {%-endfor%}
}

#[allow(dead_code)]
impl {{ie_name | UpperCamelCase }} {
    pub fn from_i32(value: i32) -> Option<Self> {
        FromPrimitive::from_i32(value)
    }
}

impl From<{{ie_name | UpperCamelCase }}> for i32 {
    fn from(s: {{ie_name | UpperCamelCase }}) -> i32 {
        s as i32
    }
}

impl TryFrom<i32> for {{ie_name | UpperCamelCase }} {
    type Error = String;
    fn try_from(v: i32) -> Result<Self, <Self as TryFrom<i32>>::Error> {
        {{ie_name | UpperCamelCase }}::from_i32(v).ok_or_else(|| format!("Invalid {{ie_name | UpperCamelCase }} value: {}", v))
    }
}


impl fmt::Display for {{ie_name | UpperCamelCase }} {
    fn fmt(&self, f: &mut fmt::Formatter) -> fmt::Result {
       write!(f, "{:?}", self)
    }
}
{%endfor%}

{% from "partials/payloads.jinja2" import arg_members, validate_schema_impl %}




// --- INTERFACE STRUCTURES ---

{%for struct_name, struct in stinger.structs.items() %}
#[allow(dead_code, non_snake_case)]
#[derive(Clone, Debug, Serialize, Deserialize, PartialEq)]
pub struct {{struct_name | UpperCamelCase}} {
    {{ arg_members(struct.values) }}
}
{{ validate_schema_impl(struct_name | UpperCamelCase, struct.values) }}
{%endfor%}

// ---- METHODS ----

{%for method_name, method in stinger.methods.items()%}

// Structures for `{{method_name}}` method

{% if not method.request_payload.is_protobuf -%}
#[allow(dead_code, non_snake_case)]
#[derive(Clone, Debug, Serialize, Deserialize)]
/// Request Object for `{{method_name}}` method.
pub struct {{method.request_payload.rust.class_name}} {
    {{ arg_members(method.arg_list) }}
}
{{ validate_schema_impl((method_name | UpperCamelCase) ~ "RequestObject", method.arg_list) }}


#[allow(dead_code)]
#[derive(Debug, Clone, Serialize, Deserialize)]
/// Return Object for `{{method_name}}` method.
pub struct {{method.rust.return_struct_name}} {
    {{ arg_members(method.return_arg_list) }}
}
{{ validate_schema_impl(method.rust.return_struct_name, method.return_arg_list) }}
{%- endif %}
{%-endfor%}

// ---- SIGNALS ----
{%for sig_name, sig in stinger.signals.items()%}
// Structures for `{{sig_name}}` signal
{% if not sig.payload.is_protobuf -%}
#[allow(dead_code, non_snake_case)]
#[derive(Clone, Debug, Serialize, Deserialize)]
pub struct {{sig.payload.rust.class_name}} {
    {{ arg_members(sig.arg_list) }}
}
{{ validate_schema_impl((sig_name | UpperCamelCase) ~ "SignalPayload", sig.arg_list) }}
{%- endif %}
{%endfor%}

// ---- COMMANDS ----
{%for cmd_name, cmd in stinger.commands.items()%}
// Structures for `{{cmd_name}}` command
{% if not cmd.payload.is_protobuf -%}
#[allow(dead_code, non_snake_case)]
#[derive(Clone, Debug, Serialize, Deserialize)]
pub struct {{cmd.rust.payload_struct_name}} {
    {{ arg_members(cmd.arg_list) }}
}
{{ validate_schema_impl(cmd.rust.payload_struct_name, cmd.arg_list) }}
{%- endif %}
{%endfor%}

{%for prop_name, prop in stinger.properties.items()%}
// `{{prop_name}}` property structure.
{% if not prop.payload.is_protobuf -%}
#[allow(dead_code, non_snake_case)]
#[derive(Clone, Debug, Serialize, Deserialize, PartialEq)]
pub struct {{prop.payload.rust.class_name}} {
    {{ arg_members(prop.arg_list) }}
}
{{ validate_schema_impl((prop_name | UpperCamelCase) ~ "Property", prop.arg_list) }}
{%- endif %}
{%endfor%}


// ---- WIRE ENCODING ----
//
// One implementation per payload, so `message.rs` can be generic over how a
// payload is encoded rather than assuming JSON.
{{ wire_impls(stinger) }}

#[cfg(test)]
mod tests {
    use super::*;
    #[allow(unused_imports)]
    use chrono::{DateTime, Utc};

    {%for prop_name, prop in stinger.properties.items()%}
    {%- if not prop.payload.is_protobuf %}{#- a protobuf property has no JSON form to round-trip #}
    #[test]
    fn test_{{prop_name | snake_case}}_property_json_format() {
        // Test deserializing from a known JSON string of {{prop.value.arg_type.name}}.
        let json_str = r#"{ "{{prop.value.name}}": {{prop.value.get_random_example_value(lang="json")}} }"#;

        let _parsed: {{prop.payload.rust.class_name}} = serde_json::from_str(json_str).unwrap();
    }
    {%- endif %}
    {%endfor%}

    {% if stinger.uses_binary() %}
    #[test]
    fn test_base64_binary_format_serialization() {
        use serde::{Deserialize, Serialize};
        
        #[derive(Serialize, Deserialize, Debug, PartialEq)]
        struct TestStruct {
            #[serde(with = "base64_binary_format")]
            data: Vec<u8>,
        }

        // Test with various binary data
        let test_data = vec![0x00, 0x01, 0x02, 0xFF, 0xFE, 0x42, 0x13, 0x37];
        let test_struct = TestStruct { data: test_data.clone() };

        // Test serialization
        let serialized = serde_json::to_string(&test_struct).unwrap();
        
        // The base64 encoded value of [0x00, 0x01, 0x02, 0xFF, 0xFE, 0x42, 0x13, 0x37] should be "AAEC//5CEzc="
        assert!(serialized.contains("AAEC//5CEzc="));

        // Test deserialization
        let deserialized: TestStruct = serde_json::from_str(&serialized).unwrap();
        assert_eq!(deserialized.data, test_data);
    }

    #[test]
    fn test_base64_binary_format_option_serialization() {
        use serde::{Deserialize, Serialize};
        
        #[derive(Serialize, Deserialize, Debug, PartialEq)]
        struct TestStruct {
            #[serde(serialize_with = "base64_binary_format::serialize_option")]
            #[serde(deserialize_with = "base64_binary_format::deserialize_option")]
            data: Option<Vec<u8>>,
        }

        // Test with Some data
        let test_data = vec![0x48, 0x65, 0x6C, 0x6C, 0x6F]; // "Hello" in bytes
        let test_struct_some = TestStruct { data: Some(test_data.clone()) };

        let serialized_some = serde_json::to_string(&test_struct_some).unwrap();
        let deserialized_some: TestStruct = serde_json::from_str(&serialized_some).unwrap();
        assert_eq!(deserialized_some.data, Some(test_data));

        // Test with None
        let test_struct_none = TestStruct { data: None };
        let serialized_none = serde_json::to_string(&test_struct_none).unwrap();
        let deserialized_none: TestStruct = serde_json::from_str(&serialized_none).unwrap();
        assert_eq!(deserialized_none.data, None);
    }

    #[test]
    fn test_base64_binary_format_round_trip() {
        use serde::{Deserialize, Serialize};
        
        #[derive(Serialize, Deserialize, Debug, PartialEq)]
        struct BinaryData {
            #[serde(with = "base64_binary_format")]
            payload: Vec<u8>,
            name: String,
        }

        // Test with empty data
        let empty_data = BinaryData {
            payload: vec![],
            name: "empty".to_string(),
        };
        let json = serde_json::to_string(&empty_data).unwrap();
        let parsed: BinaryData = serde_json::from_str(&json).unwrap();
        assert_eq!(parsed, empty_data);

        // Test with random binary data
        let random_data = BinaryData {
            payload: vec![0xDE, 0xAD, 0xBE, 0xEF, 0xCA, 0xFE, 0xBA, 0xBE],
            name: "random".to_string(),
        };
        let json = serde_json::to_string(&random_data).unwrap();
        let parsed: BinaryData = serde_json::from_str(&json).unwrap();
        assert_eq!(parsed, random_data);
    }
    {% endif %}
}
